/** Persisted by middleware when visiting `/{locale}/...` paths; read for bare-path redirects. */
export const APP_LOCALE_COOKIE = 'app_locale'
export const APP_LOCALE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365
